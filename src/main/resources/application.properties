# Vision API key: NEVER commit a real key. In production set via env (e.g. GOOGLE_VISION_API_KEY).
# Tests only need a placeholder (VisionService is mocked).
%test.google.vision.api.key=CI-PLACEHOLDER-NOT-A-REAL-KEY

# HTTP (Dev & GCF): Port 8080
quarkus.http.port=8080